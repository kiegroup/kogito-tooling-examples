{
  "name": "kogito-tooling-examples",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "init": "npx lerna bootstrap",
    "build": "npx lerna run build --stream",
    "build:fast": "npx lerna run build:fast --stream",
    "build:prod": "npx lerna run build:prod --stream",
    "update-version-to": "node ./update_version_to.js"
  },
  "devDependencies": {
    "@types/react": "16.8.8",
    "css-loader": "^4.2.1",
    "file-loader": "4.2.0",
    "lerna": "3.18.3",
    "raw-loader": "^4.0.1",
    "sass": "^1.26.10",
    "sass-loader": "^9.0.3",
    "style-loader": "^1.2.1",
    "url-loader": "^4.1.0"
  },
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "resolutions": {
    "@patternfly/react-styles": "4.4.2"
  }
}
